// utilities/validateSequence.ts

// ensure Daemon sequences are solvable in given matrix with given validation constraints
// export function validateSequence(sequence, matrix): boolean {
//   return
// }
